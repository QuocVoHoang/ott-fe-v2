export enum Navigation {
  CHAT = 'chat',
  CONTACT = 'contact',
  SETTING = 'setting'
}

export enum ModalType {
  DELETECHAT = 'delete',
  NEWCHAT = 'new',
  UPDATECHAT = 'update',
  UPFILE = 'upfile',
  USER = 'user'
}

export enum MessageType {
  TEXT = 'text', 
  IMAGE = 'image', 
  FILE = 'file', 
  AUDIO = 'audio', 
  VIDEO = 'video'
}

export enum GroupType {
  PRIVATE = 'private',
  GROUP = 'group'
}
