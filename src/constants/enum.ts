export enum Navigation {
  CHAT = 'chat',
  CONTACT = 'contact'
}

export enum MessageType {
  TEXT = 'text', 
  IMAGE = 'image', 
  FILE = 'file', 
  AUDIO = 'audio', 
  VIDEO = 'video'
}

export enum GroupType {
  PRIVATE = 'private',
  GROUP = 'group'
}
